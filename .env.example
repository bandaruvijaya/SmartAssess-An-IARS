# Google Gemini API
GEMINI_API_KEY=your_google_gemini_api_key_here

# Database (Railway will provide these as environment variables)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=smartassess

# Optional: Local embedding model path (if downloading fails)
EMBED_MODEL_PATH=all-MiniLM-L6-v2

# Flask
FLASK_ENV=production
FLASK_APP=app.py
SECRET_KEY=change_this_to_secure_random_key_in_production

# Proxy (if behind corporate proxy)
# HTTP_PROXY=http://proxy:port
# HTTPS_PROXY=http://proxy:port
